# Editable PPTX output

Generated file: `outputs/nav-system-value/nav_system_value_editable.pptx`

Note: the PPTX is a generated binary artifact and is intentionally ignored by git because PR review does not support binary diffs. Run the command below to regenerate it locally.

The deck recreates the uploaded reference image as a blue/white business-style slide using PowerPoint-native editable text boxes, rectangles, lines, arrows, table-like grids, and simple vector icons. It intentionally avoids placing a full-page screenshot as the slide background.

Regenerate it with:

```bash
python3 scripts/create_nav_system_value_ppt.py
```
